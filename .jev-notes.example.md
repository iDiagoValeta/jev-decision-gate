Copy to `.jev-notes.md` in this project (gitignored, never commit it) to
give Jev project-scoped context. For preferences that should apply across
every project, write `$XDG_CONFIG_HOME/jev-gate/notes.md` (defaults to
`~/.config/jev-gate/notes.md`) instead — both are read and shown to Jev
together when present (see `load_user_notes` in `src/jev_gate/schemas.py`).

This is evidence Jev weighs alongside the objective and the halt detail,
never a rule engine: it cannot force an allow, and it never runs before or
instead of Jev — the catastrophic kill-list (plugin/jev-decision-gate/index.ts)
and the ask-human/fail-open defaults apply exactly the same regardless of
what you write here. Plain text, freeform, whatever helps Jev judge like
you would:

Example:

    I'm the only one working in this repo; you don't need to be cautious
    about clobbering someone else's uncommitted work here.
    Installing new Python/npm dependencies is fine without asking.
    Anything that touches CI config or branch protection: always ask-human,
    even if it looks safe.
